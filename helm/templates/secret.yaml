apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
data:
  DB_PASSWORD: dGVzdA==   # Base64 encoded password (echo -n 'test' | base64)
  SECRET_KEY: c2VjcmV0IGtleQ== # Base64 encoded secret key (echo -n 'secret key' | base64)
